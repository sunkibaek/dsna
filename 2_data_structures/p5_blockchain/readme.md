The significance in the problem is the block made later is linked to the
previous block using hash value.

Adding a block to another block is a task of assigning the pointer so takes
O(1) time to accomplish.
