Iterate the list to find min and max. To determine min see if the element is
smaller than saved min. The same logic applies to max.

To iterate through the list it takes O(n) in time and O(1) in space.
